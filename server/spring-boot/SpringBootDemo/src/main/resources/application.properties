server.port=6789
# The demo starts without a security filter until a KeyProvider is configured.
# Never put session keys in this file.
spring.sc.enabled=${SC_ENABLED:false}
spring.sc.legacy.enabled=false
